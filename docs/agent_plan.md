# Agent plan

This working plan is maintained at the repo root as [../agent_plan.md](../agent_plan.md).

It defines the workstreams, ownership boundaries, and handoff rules for multi-agent delivery so the public website, admin dashboard, backend service, and infrastructure work do not overwrite one another.
